# ARDA-INBOX

## 2026-09-23
Stüdyo kuruldu, çalışıyor. Bu branch (`claude/app-studio`) vault'undan tamamen ayrı.
Mac'te almak için:

    git clone -b claude/app-studio --single-branch https://github.com/ardaerturkk/ardavault.git app-studio

Hazır olan her uygulama için `apps/<app>/ARDA-MAC.md` yazılacak. Durdurmak için bu branch'e
`STOP` adında bir dosya koy. Bundle ID öneki şimdilik `com.arda`; istersen değiştir.
